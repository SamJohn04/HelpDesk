package HelpDesk;

public class Request {
   String randId, userName, userRollNum, userBranch, userSem, requestTitle, requestDescription, userEmail;
   int userBatch;
   
   public Request(/*All instance variables except Id*/) {
    /*
     * Main body
     * 
     * 
     */
    generateId();
    }

   private void generateId() {
   }
}
